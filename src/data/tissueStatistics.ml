type t = { healthy_cells_capacity : int;
                    healthy_cells : int;
                     cancer_cells : int;
                        is_cloted : bool;
                         is_outed : bool;
         }

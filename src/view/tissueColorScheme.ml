type t = {  clot : Graphics.color;
           virus : Graphics.color;
            line : Graphics.color; 
           white : Graphics.color;
            blue : Graphics.color;
            gray : Graphics.color
         }

let default =  {  clot = Color.red;
                 virus = Color.yellow;
                  line = Color.black;  
                 white = Color.white;
                  blue = Color.blue;
                  gray = Color.gray 
               }
